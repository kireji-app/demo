const scrollbar = echo('scroll-bar')

/*const
 max = 1114111,
 output = say(`<output>&#${0};`)[0],
 input = say(`<input type=range min=0 max=${max} value=${max / 2} step=1>`)[0];

input.oninput = () => {
 output.innerHTML = `&#${input.value};`
}

warning({width, height})

*/