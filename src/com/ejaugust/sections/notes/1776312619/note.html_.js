return `<p>The ecosystem now has an <a ${_.pointAttr()} href="https://issue-tracker.kireji.app">embedded issue tracker</a>. It comes as a child application to the <a ${_.pointAttr()} href="https://kireji.app">Kireji Part Viewer</a> and it will integrate directly with it.</p>
<p>Incidentally, there are <em>issues</em> related the issue tracker. I haven't finished populating it with all of the work-in-progress items, known bugs, or upcoming features that I need to. I'm also not currently planning to retroactively populate it with past tasks, even though seeing all of those <code>done</code> tickets would feel good.</p>
<p>It's a minimalist approach to a task-tracking application, but I'm not afraid of iteratively enhancing it later. There are just three simple statuses for tickets/issues: <code>to-do</code>, <code>doing</code> and <code>done</code>. They can have one of three priorities: <code>A</code>, <code>B</code> and <code>C</code>. Issues have titles and descriptions and can also show the list of parts they impact along with links to related issues.</p>
<p>The summary screen lets you filter issues by status and priority as well as sort the list ascending and descending by title, priority, status and creation date.</p>
<p>I'll wouldn't mind being able to integrate it with the notebook. That would allow me to reference tickets in my notes and have them render consistent way.</p>`