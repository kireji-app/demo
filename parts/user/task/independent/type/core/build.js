// when this window closes, make sure we erase the data
// this program is like explorer in windows and finder on mac
// we don't need to retain it's exact data when it closes AND
// if there are global settings like view settings
// those things aren't a child of core.parts they are a desktop setting
// etc. etc.