return `<span id="era-control" tabIndex=7 data-state="${desktop.era.arm.stateData}" onclick="_.parts.desktop.era.setRouteID(1n, true)">
 <span class="label">${desktop.era.title}:</span><flex-spacer></flex-spacer><span class="label2">${desktop.era.arm.title}</span>
 <span class="base"><span class="handle">${desktop.era.arm.unicode}</span></span>
</span>`