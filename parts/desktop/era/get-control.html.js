return `<span id="era-control" tabIndex=7 data-state="${root.parts.desktop.era.arm.stateData}" onclick="root.parts.desktop.era.setRouteID(1n, true)">
 <span class="label1">${root.parts.desktop.era.title}:</span><flex-spacer></flex-spacer><span class="label2">${root.parts.desktop.era.arm.title}</span>
 <span class="base"><span class="handle">${root.parts.desktop.era.arm.unicode}</span></span>
</span>`