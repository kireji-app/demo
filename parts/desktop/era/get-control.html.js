return `<span id="era-control" tabIndex=7 data-state="${desktop.era.arm.stateData}" onclick="desktop.era.setRouteID(1n, true)">
 <span class="label1">${desktop.era.title}:</span><flex-spacer></flex-spacer><span class="label2">${desktop.era.arm.title}</span>
 <span class="base"><span class="handle">${desktop.era.arm.unicode}</span></span>
</span>`